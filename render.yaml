services:
  - type: web
    name: redaccion-topografica-v3-1
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --threads 2 --timeout 120"
    autoDeploy: true
